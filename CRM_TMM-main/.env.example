# Example environment variables for local development
# Copy this file to .env and update values as needed (DO NOT commit secrets to git)

# Django
DJANGO_SECRET_KEY=replace-me-with-a-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Email (console backend by default) - use SMTP in production
DJANGO_EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DJANGO_DEFAULT_FROM_EMAIL=carolina@tmmbienestar.cl

